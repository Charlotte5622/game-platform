# 环境变量配置
# 复制此文件为 .env 并修改

# 服务端口
CLIENT_PORT=3000
SERVER_PORT=8080

# 数据库（如果用外部数据库，修改这里）
DATABASE_URL=postgresql://postgres:postgres@db:5432/gameplatform

# Redis
REDIS_URL=redis://redis:6379

# JWT 密钥（用于用户认证）
JWT_SECRET=change-this-to-random-string

# 服务器域名（部署时修改）
SERVER_DOMAIN=localhost
